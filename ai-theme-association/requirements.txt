# Data
pandas>=2.2
numpy>=1.26
pyarrow>=15.0          # parquet cache
pyyaml>=6.0

# Market data
yfinance>=0.2.50
pandas-datareader>=0.10    # Kenneth French factor library
exchange-calendars>=4.5    # NYSE trading calendar, for price validation

# Stats
statsmodels>=0.14          # OLS, HAC/Newey-West standard errors
scipy>=1.12
scikit-learn>=1.4          # TF-IDF dedup, PCA for the latent AI factor

# Text
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0
# sentence-transformers>=3.0  # embeddings tier was cut (see src/classify/embed.py);
#                             # left commented because it pulls ~2 GB of torch for nothing

# LLM
anthropic>=0.40

# Plotting
matplotlib>=3.8

# Notebooks
jupyter>=1.0
